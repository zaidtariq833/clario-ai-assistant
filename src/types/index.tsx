export type FAQ = {
  question: string;
  answer: string;
};

export type Steps = {
  name: string;
  mainText: string;
  image: string;
  btnText: string;
};

export type AccordionType = {
  state: boolean;
  answer: string;
};
