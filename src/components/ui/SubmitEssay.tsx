import { geistRegular } from "@/styles/fonts";

export default function SubmitEssay() {
  return (
    <button className={`${geistRegular.className} btn-submitessay`}>
      Submit & Get Report
    </button>
  );
}
